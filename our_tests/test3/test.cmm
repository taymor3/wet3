# Testing arithmitcal manipluation between real number
void test (a,b,c:float) {
    res:float;
    res = (a+b) + (b*c*a + (b + 9.0))/3.0;

    write(res);
}

void main() {
     a,b,c:float;
     read(a);
     read(b);
     read(c);
     test(a,b,c);
}