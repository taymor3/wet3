# Testing arithmitcal manipluation between integers
int test (a,b,c:int) {
    return (a+b) + (b*c*a + (b + 9))/3;
}

void main() {
     a,b,c:int;
     read(a);
     read(b);
     read(c);
     write(test(a,b,c));
}